# Project Agent Notes

This repository is a monorepo for a fullstack admin system.

- Frontend: `apps/admin-web`
- Backend: `apps/api-server`
- Shared package: `packages/shared`
- Database schema: `prisma/schema.prisma`
- Deployment assets: `docker`, `nginx`, `.github/workflows`

Prefer workspace scripts from the repository root.
